// Define the Product type (adjust according to your actual product structure)
export interface Product {
  id: string;
  title: string;
  price: string;
  images: string[];
  description: string;
  // Add other product properties as needed
}